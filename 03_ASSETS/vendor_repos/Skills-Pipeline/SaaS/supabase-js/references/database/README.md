# Database Reference — Router

This folder contains Supabase Database documentation split into four files. Read the one(s) relevant to the user's request.

## Routing

| User's request involves... | Read this file |
|---|---|
| Connecting to the database, connection strings, poolers, SSL, importing data, securing data, Data APIs | `fundamentals.md` |
| Creating tables, columns, data types, primary keys, loading data, foreign keys, schemas, views, materialized views, arrays, indexes, joins (one-to-many, many-to-many, inner join, left join), JSON/JSONB | `basics.md` |
| Cascade deletes, enums, database functions (`create function`, `rpc()`), triggers, database webhooks, event triggers | `intermediate.md` |
| Row Level Security (RLS), policies, Column Level Security (CLS), Postgres roles (`anon`, `authenticated`, `service_role`), custom roles, `auth.uid()`, `auth.jwt()` | `access-security.md` |

If the request spans multiple areas, read multiple files.